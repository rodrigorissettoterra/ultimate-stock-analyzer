name: bcb-ifdata-probe

on:
  pull_request:
    paths:
      - ".github/workflows/bcb-ifdata-probe.yml"
      - "scripts/bcb_ifdata_probe.py"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install project
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      - name: Probe official BCB IFData contract
        run: python scripts/bcb_ifdata_probe.py
